spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:55432/asterion_auth
    username: asterion
    password: asterion

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.SQL: debug
    org.springframework.security: info

security:
  jwt:
    secret: very-secret-development-key-change-in-production-123456789
    expiration-seconds: 3600
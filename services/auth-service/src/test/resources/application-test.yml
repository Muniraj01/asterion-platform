asterion:
  security:
    bootstrap-admin:
      password: test-bootstrap-admin-password
    internal:
      services:
        api-gateway:
          token: test-internal-service-token
          permissions:
            - USER_READ

        reporting-service:
          token: test-reporting-service-token
          permissions: [ ]